server:
  port: 9190
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mnm_db
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: yourEmail
    password: yourPassword
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
